var teacherModel = require('./teacher_model'),
    packageModel = require('./package_model'),
    clientModel = require('./client_model'),
    subjectModel = require('./subject_model')
    scheduleModel = require('./schedule_model'),
    paymentModel = require('./payment_model');

module.exports = { teacherModel, clientModel, packageModel, subjectModel, scheduleModel, paymentModel }
